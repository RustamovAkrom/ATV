# LOCAL MODE (enabled by default)
server {
    listen 80;
    server_name _;

    # Docker embedded DNS для динамического резолвинга имён контейнеров
    resolver 127.0.0.11 ipv6=off valid=10s;
    set $backend "web:8000";

    # Proxy таймауты
    proxy_connect_timeout 30s;
    proxy_send_timeout 30s;
    proxy_read_timeout 30s;

    location /static/ {
        alias /app/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /storage/ {
        alias /app/storage/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # API routes: /api/, /docs, /redoc, /admin, /openapi.json
    location / {
        proxy_pass http://$backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# PROD MODE (commented out)
# server {
#     listen 80;
#     server_name _;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl;
#     server_name _;
#
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#
#     # Docker embedded DNS
#     resolver 127.0.0.11 ipv6=off valid=10s;
#     set $backend "web:8000";
#
#     # Proxy таймауты
#     proxy_connect_timeout 30s;
#     proxy_send_timeout 30s;
#     proxy_read_timeout 30s;
#
#     location /static/ {
#         alias /app/static/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /storage/ {
#         alias /app/storage/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     # API routes: /api/, /docs, /redoc, /admin, /openapi.json
#     location / {
#         proxy_pass http://$backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
#
# server {
#     listen 443 ssl;
#     server_name _;
#
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#
#     # Docker embedded DNS
#     resolver 127.0.0.11 ipv6=off valid=10s;
#     set $backend "web:8000";
#
#     # Proxy таймауты
#     proxy_connect_timeout 30s;
#     proxy_send_timeout 30s;
#     proxy_read_timeout 30s;
#
#     location /static/ {
#         alias /app/static/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /storage/ {
#         alias /app/storage/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     # API routes: /api/, /docs, /redoc, /admin, /openapi.json
#     location / {
#         proxy_pass http://$backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection "";
#     }
# }
